extends layout

block content
  header.global-header
    .container-fluid
      .row
        .col-xs-12
          a.header-logo
            img(src="/triage/static/imgs/njc-logo.png")
            h1.header-title NJC FV Cases
  main
    .page(ui-view)
  footer.global-footer
    .container-fluid
      .row
        .col-xs-12
  script.
    angular.module('njcTriage')
      .constant('Constants',  Object.freeze({
        'urls': {
          'api':'#{appConfig.urls.api}',
          'public':'#{appConfig.urls.public}'
        }
      }));
